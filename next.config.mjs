/** @type {import('next').NextConfig} */
const nextConfig = { distDir: 'build',};
// module.exports = {
//     distDir: 'build', // Change the build folder name to "build"
// };
export default nextConfig;
